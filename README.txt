COPY THESE FILES into the same folder as your existing config.js:

admin.html
verify.html
digital-id.html
styles.css

IMPORTANT:
Your existing config.js must contain your real Google Apps Script /exec URL:

const KK_CONFIG = {
    API_URL: "YOUR_REAL_APPS_SCRIPT_EXEC_URL"
};

Do NOT put ADMIN_PASSWORD in GitHub.

After copying the files, commit and push them to GitHub.
